
# 리스트 내 모든 원소의 합
result = sum([1, 2, 3])
print(result)

# 최소, 최대값 찾는 함수
result = min(1, 2, 3)  # arg를 하나씩 넣을 수도 있고
print(result)
# or
list = [1, 2, 3]
result = min(list)  # 리스트로 넣을수도 있다.
print(result)
